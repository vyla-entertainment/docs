---
title: Quickstart
description: From zero to a working HLS player in under 5 minutes.
---

## Before You Start

You'll need a TMDB ID for the content you want to stream. Every movie and TV show on [themoviedb.org](https://www.themoviedb.org) has one — it's the number in the URL.

| URL | TMDB ID | Title |
|-----|---------|-------|
| `themoviedb.org/movie/550` | `550` | Fight Club |
| `themoviedb.org/movie/27205` | `27205` | Inception |
| `themoviedb.org/tv/1396` | `1396` | Breaking Bad |
| `themoviedb.org/tv/94997` | `94997` | House of the Dragon |

---

## Step 1 — Fetch Sources

Call `/api/movie` or `/api/tv`. The API fans out to all providers in parallel, verifies every URL live, and returns only working sources.

<CodeGroup>
```bash Movie
curl https://missourimonster-vyla-api.hf.space/api/movie?id=550
```

```bash TV Episode
curl "https://missourimonster-vyla-api.hf.space/api/tv?id=1396&season=1&episode=1"
```
</CodeGroup>

<Accordion title="Example response">
```json
{
  "sources": [
    {
      "source": "source1",
      "label": "Source1",
      "url": "/api?url=https%3A%2F%2F...&s1=1"
    },
    {
      "source": "source2",
      "label": "Source2",
      "url": "/api?url=https%3A%2F%2F...&s2=1"
    }
  ],
  "subtitles": [
    {
      "label": "English",
      "file": "https://cache.vdrk.site/v1/vtt/movie/550/English.vtt"
    },
    {
      "label": "Spanish",
      "file": "https://cache.vdrk.site/v1/vtt/movie/550/Spanish.vtt"
    }
  ],
  "meta": {
    "id": 550,
    "title": "Fight Club",
    "release_date": "1999-10-15"
  }
}
```
</Accordion>

---

## Step 2 — Play with HLS.js

The `url` field on each source is already proxied, CORS-safe, and ready to pass directly to your player.

<Tabs>
  <Tab title="Vanilla JS">
    ```html
    <!DOCTYPE html>
    <html>
    <head>
      <script src="https://cdn.jsdelivr.net/npm/hls.js@latest/dist/hls.min.js"></script>
    </head>
    <body>
      <video id="player" controls style="width:100%"></video>

      <script>
        const BASE = 'https://missourimonster-vyla-api.hf.space';

        async function loadMovie(tmdbId) {
          const res  = await fetch(`${BASE}/api/movie?id=${tmdbId}`);
          const data = await res.json();
          if (!data.sources?.length) return console.error('No sources');

          const video = document.getElementById('player');
          const hls   = new Hls();

          hls.loadSource(BASE + data.sources[0].url);
          hls.attachMedia(video);

          hls.on(Hls.Events.MANIFEST_PARSED, () => {
            // Attach subtitle tracks
            data.subtitles.forEach(sub => {
              const track  = document.createElement('track');
              track.kind   = 'subtitles';
              track.label  = sub.label;
              track.src    = sub.file;
              video.appendChild(track);
            });
            video.play();
          });
        }

        loadMovie(550);
      </script>
    </body>
    </html>
    ```
  </Tab>
  <Tab title="React">
    ```tsx
    import { useEffect, useRef } from 'react';
    import Hls from 'hls.js';

    const BASE = 'https://missourimonster-vyla-api.hf.space';

    export function Player({ tmdbId }: { tmdbId: number }) {
      const videoRef = useRef<HTMLVideoElement>(null);

      useEffect(() => {
        let hls: Hls;

        async function load() {
          const res  = await fetch(`${BASE}/api/movie?id=${tmdbId}`);
          const data = await res.json();
          if (!data.sources?.length || !videoRef.current) return;

          hls = new Hls();
          hls.loadSource(BASE + data.sources[0].url);
          hls.attachMedia(videoRef.current);

          hls.on(Hls.Events.MANIFEST_PARSED, () => {
            videoRef.current?.play();
          });
        }

        if (Hls.isSupported()) load();
        return () => hls?.destroy();
      }, [tmdbId]);

      return <video ref={videoRef} controls style={{ width: '100%' }} />;
    }
    ```
  </Tab>
  <Tab title="Next.js">
    ```tsx
    'use client';
    import { useEffect, useRef } from 'react';
    import dynamic from 'next/dynamic';

    const BASE = 'https://missourimonster-vyla-api.hf.space';

    export default function Player({ tmdbId }: { tmdbId: number }) {
      const videoRef = useRef<HTMLVideoElement>(null);

      useEffect(() => {
        // Dynamic import keeps Hls out of SSR bundle
        import('hls.js').then(({ default: Hls }) => {
          if (!Hls.isSupported() || !videoRef.current) return;

          fetch(`${BASE}/api/movie?id=${tmdbId}`)
            .then(r => r.json())
            .then(data => {
              if (!data.sources?.length) return;
              const hls = new Hls();
              hls.loadSource(BASE + data.sources[0].url);
              hls.attachMedia(videoRef.current!);
            });
        });
      }, [tmdbId]);

      return <video ref={videoRef} controls className="w-full" />;
    }
    ```
  </Tab>
  <Tab title="Python">
    ```python
    import requests

    BASE = "https://missourimonster-vyla-api.hf.space"

    def get_sources(tmdb_id: int, season: int = None, episode: int = None):
        if season and episode:
            url = f"{BASE}/api/tv"
            params = {"id": tmdb_id, "season": season, "episode": episode}
        else:
            url = f"{BASE}/api/movie"
            params = {"id": tmdb_id}

        res = requests.get(url, params=params)
        res.raise_for_status()
        data = res.json()

        return {
            "sources": data.get("sources", []),
            "subtitles": data.get("subtitles", []),
        }

    # Example
    result = get_sources(550)
    for source in result["sources"]:
        print(f"{source['label']}: {BASE}{source['url']}")
    ```
  </Tab>
</Tabs>

---

## Step 3 — Handle Fallbacks

Providers can go down at any time. Build a fallback loop so your player automatically tries the next source on failure.

```typescript
const BASE = 'https://missourimonster-vyla-api.hf.space';

async function loadWithFallback(
  videoEl: HTMLVideoElement,
  tmdbId: number,
  season?: number,
  episode?: number
) {
  const endpoint = season && episode
    ? `${BASE}/api/tv?id=${tmdbId}&season=${season}&episode=${episode}`
    : `${BASE}/api/movie?id=${tmdbId}`;

  const data = await fetch(endpoint).then(r => r.json());
  const sources: string[] = data.sources.map((s: any) => BASE + s.url);

  let index = 0;
  let hls: Hls;

  function tryNext() {
    if (index >= sources.length) {
      console.error('All sources exhausted');
      return;
    }

    hls?.destroy();
    hls = new Hls();

    hls.on(Hls.Events.ERROR, (_, err) => {
      if (err.fatal) {
        console.warn(`Source ${index} failed, trying next`);
        index++;
        tryNext();
      }
    });

    hls.loadSource(sources[index]);
    hls.attachMedia(videoEl);
    hls.on(Hls.Events.MANIFEST_PARSED, () => videoEl.play());
  }

  tryNext();
}
```

---

## Step 4 — Add Subtitles

Subtitles are included automatically in movie and TV responses. You can also fetch them standalone if you're building a UI that loads them separately.

```javascript
// Already included in /api/movie and /api/tv responses
const { sources, subtitles } = await fetch(
  'https://missourimonster-vyla-api.hf.space/api/movie?id=550'
).then(r => r.json());

// Or fetch standalone
const subtitles = await fetch(
  'https://missourimonster-vyla-api.hf.space/api/subtitles/movie/550'
).then(r => r.json());

// Apply to video element
subtitles.forEach(sub => {
  const track = Object.assign(document.createElement('track'), {
    kind:  'subtitles',
    label: sub.label,
    src:   sub.file,
  });
  videoEl.appendChild(track);
});
```

---

## Check Provider Health

Before building, verify which providers are currently live:

```bash
curl https://missourimonster-vyla-api.hf.space/api/health
```

```json
{
  "status": "ok",
  "timestamp": "2025-01-01T00:00:00.000Z",
  "sources": {
    "source1":  { "ok": true,  "ms": 1204 },
    "source2": { "ok": true,  "ms": 980  },
    "source3":  { "ok": false, "ms": 435  },
    "source4": { "ok": true,  "ms": 1540 },
    "source5": { "ok": true,  "ms": 1100 },
    "source6":  { "ok": true,  "ms": 620  }
  }
}
```

`status: "degraded"` means at least one provider is down. The rest still work — your fallback logic handles it automatically.

---

## Debug a Single Provider

Use `/api/test` to check a specific provider without going through the full fanout:

```bash
# Test Source1 for Fight Club
curl "https://missourimonster-vyla-api.hf.space/api/test/550?source=source1"

# Test Source2 for Breaking Bad S1E1
curl "https://missourimonster-vyla-api.hf.space/api/test/1396?season=1&episode=1&source=source2"
```

<Card title="Full API Reference →" href="/api-reference/movie" icon="book">
  See complete request/response docs for every endpoint.
</Card>