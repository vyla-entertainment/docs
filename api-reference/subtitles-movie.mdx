---
title: Movie Subtitles
api: "GET https://missourimonster-vyla-api.hf.space/api/subtitles/movie/{tmdb_id}"
description: Fetch all available WebVTT subtitle tracks for a movie.
---

Returns an array of subtitle tracks for a movie. Each track is a direct link to a `.vtt` file — no proxy needed. Subtitle tracks are also bundled automatically in `/api/movie` responses; use this endpoint when you need them independently.

---

## Path Parameters

<ParamField path="tmdb_id" type="string" required>
  TMDB movie ID.

  **Example:** `/api/subtitles/movie/550` → subtitles for Fight Club
</ParamField>

---

## Request

<CodeGroup>
```bash cURL
curl https://missourimonster-vyla-api.hf.space/api/subtitles/movie/550
```

```javascript JavaScript
const subtitles = await fetch(
  'https://missourimonster-vyla-api.hf.space/api/subtitles/movie/550'
).then(r => r.json());

subtitles.forEach(sub => {
  console.log(`${sub.label}: ${sub.file}`);
});
```

```typescript TypeScript
interface Subtitle {
  label: string;
  file: string;
}

const subtitles: Subtitle[] = await fetch(
  'https://missourimonster-vyla-api.hf.space/api/subtitles/movie/550'
).then(r => r.json());
```

```python Python
import requests

subtitles = requests.get(
  'https://missourimonster-vyla-api.hf.space/api/subtitles/movie/550'
).json()

for sub in subtitles:
    print(f"{sub['label']}: {sub['file']}")
```
</CodeGroup>

---

## Response

Returns a JSON array. `404` if no subtitles are found for this title.

<ResponseField name="[]" type="Subtitle[]">
  Array of subtitle track objects.

  <Expandable title="Subtitle object">
    <ResponseField name="label" type="string">
      Language name of the track, e.g. `English`, `Spanish`, `Arabic`, `French`, `Portuguese`.
    </ResponseField>
    <ResponseField name="file" type="string">
      Direct URL to a WebVTT (`.vtt`) subtitle file. Accessible without any proxy or authentication.
    </ResponseField>
  </Expandable>
</ResponseField>

---

## Responses

<Tabs>
  <Tab title="200 — Success">
    ```json
    [
      {
        "label": "English",
        "file": "https://cache.vdrk.site/v1/vtt/movie/550/English.vtt"
      },
      {
        "label": "Spanish",
        "file": "https://cache.vdrk.site/v1/vtt/movie/550/Spanish.vtt"
      },
      {
        "label": "French",
        "file": "https://cache.vdrk.site/v1/vtt/movie/550/French.vtt"
      },
      {
        "label": "Arabic",
        "file": "https://cache.vdrk.site/v1/vtt/movie/550/Arabic.vtt"
      },
      {
        "label": "Portuguese",
        "file": "https://cache.vdrk.site/v1/vtt/movie/550/Portuguese.vtt"
      }
    ]
    ```
  </Tab>
  <Tab title="404 — Not Found">
    No subtitle tracks were found for this TMDB ID.

    ```json
    { "error": "no subtitles found" }
    ```
  </Tab>
  <Tab title="500 — Server Error">
    ```json
    { "error": "<error message>" }
    ```
  </Tab>
</Tabs>

---

## Usage Examples

### HTML `<track>` element

```html
<video id="player" controls>
  <!-- Subtitle tracks added dynamically -->
</video>

<script>
  fetch('https://missourimonster-vyla-api.hf.space/api/subtitles/movie/550')
    .then(r => r.json())
    .then(subs => {
      const video = document.getElementById('player');
      subs.forEach((sub, i) => {
        const track = document.createElement('track');
        track.kind    = 'subtitles';
        track.label   = sub.label;
        track.src     = sub.file;
        track.default = i === 0; // default to first track
        video.appendChild(track);
      });
    });
</script>
```

### Video.js

```javascript
import videojs from 'video.js';

const subs = await fetch(
  'https://missourimonster-vyla-api.hf.space/api/subtitles/movie/550'
).then(r => r.json());

const player = videojs('player');

subs.forEach(sub => {
  player.addRemoteTextTrack({
    kind: 'subtitles',
    label: sub.label,
    src: sub.file,
  }, false);
});
```

### Plyr

```javascript
import Plyr from 'plyr';

const subs = await fetch(
  'https://missourimonster-vyla-api.hf.space/api/subtitles/movie/550'
).then(r => r.json());

const player = new Plyr('#player', {
  captions: { active: true, update: true },
});

// Plyr reads <track> elements — add them before initialising
subs.forEach(sub => {
  const track = Object.assign(document.createElement('track'), {
    kind:  'captions',
    label: sub.label,
    src:   sub.file,
  });
  document.getElementById('player').appendChild(track);
});
```

<Note>
  Subtitle tracks are included automatically in `/api/movie` responses. Only use this endpoint when you need subtitles without fetching stream sources.
</Note>